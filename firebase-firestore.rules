rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /darumaWishes/{docId} {
      allow read, write: if request.auth != null
        && (docId == request.auth.uid || docId == "default");
    }
  }
}
